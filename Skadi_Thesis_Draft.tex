\documentclass[letterpaper, 12pt, oneside]{book}
\usepackage[margin = 1in, includehead, footskip=0.25in]{geometry}
\usepackage[toc,page]{appendix}
\usepackage{setspace}
\usepackage{caption}
\usepackage{subcaption}
\doublespacing
\usepackage{dutchcal}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{tikz}
\usepackage[compat=1.1.0]{tikz-feynman}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage[most]{tcolorbox}
\colorlet{LightLavender}{violet!40!}
\colorlet{transpink}{pink!80!}
\colorlet{transblue}{blue!80!white!10!}
\tcbset{on line, 
	boxsep=4pt, left=0pt, right=0pt, top=0pt, bottom=0pt,
	colframe=black, colback=LightLavender,
	highlight math style={enhanced}
	}
\usepackage{lscape}
\usepackage{import}
\usepackage[style=phys]{biblatex}
\newcommand{\dd}{\textrm{d}}
\usepackage{mathrsfs}
\bibliography{thesis_refs}
%\addbibresource{thesis_refs.bib}
\usepackage{lineno}
\newcommand{\floor}[1]{\left\lfloor {#1} \right\rfloor}
\linenumbers
\usepackage[pdfauthor={My Full Name},
    pdftitle={Title},
    hidelinks
]{hyperref}
\makeatletter
\newtheorem{lemma}{Lemma}[section]
\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem{corolary}{Corolary}[section]
%\theoremstyle{definition} % can't quite remember how to do this, gotta look at some of my old notes
\newenvironment{algo}[1][Algorithm]
{
	\begin{centering}
	\textsc{\textbf{Algorithm:} #1}
	\\
	\hline
}
{
	\end{centering}
}

\tcolorboxenvironment{algo}{on line, 
	boxsep=4pt, left=0pt, right=0pt, top=0pt, bottom=0pt,
	colframe=black, colback=LightLavender,
	highlight math style={enhanced}
}
\newcounter{proof}[section]
\renewenvironment{proof}[1]
{

	\textbf{\textsc{Proof} \refstepcounter{proof}}
	\vspace{0.2 cm}
	\hline
	\vspace{0.2 cm}
	%\begin{centering}	

}
{
%	\end{centering}
	\vspace{0.2 cm}
	$\mathcal{Q}\mathcal{E}\mathcal{D}$
	
}
\tcolorboxenvironment{definition}{
	on line, boxsep=0pt, left=1pt, right=1pt, top=1pt, bottom=1pt,
	colframe=white, colback=transblue,
	highlight math style={enhanced}
}
%\tcolorboxenvironment{proof}{
%	 boxsep=0.1\textwidth, left=0pt, right=0pt, top=0pt, bottom=0pt,
%	colframe=white, colback=transpink,
%	highlight math style={enhanced}
%}
%\author{Skadi K Grossberndt}
%\title{Energy Correlators as a probe of the hard process in Run 24 $p-p$ collisions at $\sqrt{s}=200$ $GeV$ with the sPHENIX detector at RHIC}
%\affil{Graduate Center and Baruch College, CUNY}
\begin{document}
%\maketitle
\frontmatter
\include{Title_page}
\setcounter{page}{2}
\include{Copyright_page}
\include{Approval_page}
\include{Abstract}
\include{Acknowledgements}

\tableofcontents
\listoftables
\listoffigures

\mainmatter
\chapter{Literature Review}
\label{ch:LR}
\include{Lit_Review} 
\part{Hardware: The Detector and Simulations}
\include{Hardware_and_sim}
\part{Experimental Output: The Main Event}
\chapter{So is sPHENIX actually working?}

\chapter{Measuring the Energy Correlator}
	\section{Discretization and Efficiency}
	\include{Corrections_and_Error}
	In the discussion of the energy correlator in chapter \ref{ch:ENC_th}, we had been working in an idealized regime where the measurement is able to be resolved to arbitary size, however, this is limited by the physical reality of the detector systems.

	In simulations and data, one finds that the practical reality of making measurements of the Energy correlator is severly hamstrung by the use of tower size objects rather than the truth particles that make up our idealized toy jets discussed in section \ref{sec:toy_jet_id}. 
	To study the efficiency loss, one takes the identified truth jet objects from Monte Carlo Simulations (Pythia8 and Herwig for pp; sHIJING, EPOS4, AMPT for AuAu) and runs them through Geant simulations to model detector effects.
	One then takes the initial truth jet, draws a circle that encloses the truth particles, and projects this out onto the calibrated tower response in each calorimeter, as well as runs an anti-k$_{\textrm{T}}$ jet identification algorithm to simulate jet reconstruction.
	These three object types (truth jet, projective cone and reconstructed jet) are collected into sets and the measurement of the two- and three-point energy correlators is performed using the defintion in eq \ref{eq:calo_enc}
	\begin{equation}
		\begin{split}
			\left< E2C \left( R_L \right) \right> &= \frac{1}{N_\textrm{events}} \sum_{\textrm{events}} \frac{1}{N_\textrm{jet}} \sum_{N_{\textrm{jets}}} \sum_{i,j \in \textrm{Jet Consitutents}} \frac{E_{i} E_{j}}{E_\textrm{jets}^2} \delta \left( R_{ij} - R_L \right) \\  \\
			\left< E3C \left( R_L \right) \right> &= \frac{1}{N_\textrm{events}} \sum_{\textrm{events}} \frac{1}{N_\textrm{jet}} \sum_{N_{\textrm{jets}}} \sum_{i,j,k \in \textrm{Jet Consitutents}} \frac{E_{i} E_{j} E_{k}}{E_\textrm{jets}^3} \delta \left( \textrm{max}{R_{i,j,k}} - R_L \right) 
		\end{split}
		\label{eq:calo_enc}
	\end{equation}
	
	\section{Whole Calorimeter Measurements}
	While the Energy correlator is generally studied within a jet, one can make use of the whole calorimeter to compare the energy correlator across all angular seperations to characterize the underlying event. 
	This induces a new computational issue of scale, where the number of calculations goes from $_{50}C_2=1225$ in the HCAL and $_{804} C_2=322,806$ in the EMCAL to $_{1536}C_2 \approx 2.4 \times 10^6$ and $_{24576}C_2 \approx 6 \times 10^8$ in the EMCAL.
	However, since pp events have relatively low multiplicity in the calormeters as is seen in fig \ref{fig:calo_pp_multiplicity}, one reduces the computational load signifigantly by preprocessing the towers to remove all towers with energy below some threshold, functionally mirroring the zero-suppression procedure discussed in section \ref{sec:zero_supr}.

	\begin{figure}
		\includegraphics[width=0.8\textwidth]{ohcal_occup}%put a multiplicity figure in here to justify computational load decrease
		\caption{Multiplicty in each Calorimeter (pending--have ohcal now) as percentage of towers with $E \geq 10$ MeV to filter out electronics fluctuations}
		\label{fig:calo_pp_multiplicity}
	\end{figure}
	One also notes that the whole event correlator on calorimeter towers is signfigantly sensitive to the electronic noise of the towers, given that any positive electronic noise would mimic a valid energy deposition. 
	While the zero supression reduces the noise by a signifigant fraction at the hardware level, there are fluctations that pass this cut. 
	Each event has an entry for every tower, and due to this electronic noise not having the same waveform shape in the ADC (see figs \ref{fig:hcal_ped_single_wf} and \ref{fig:hcal_data_wf} for a comparision of a pedestal waveform and that of a data event--there are a fraction of towers that have an apparent negative energy, which when summing the total energy of the calorimeters, approximately cancels out positive fluctionations on average. 
	However in our case, it is not so simple. 
	Consider the following ansatz of a symmetric distribution $S=\{-2, -2, -1, -1, -1, -1, 1, 1, 1, 1, 2, 2\}$, then one considers the sum of  the product of pairs, being the numerator of the two point energy correlator would be the only way to have the noise contribution go to zero
	\begin{equation}
		\begin{split}
			PS & = (-2)^2 + (-2\cdot-1) \times 4 + (-2 \cdot 1) \times 4 + (-2\cdot 2) \times 2  \\ 
			& \hspace{0.5 cm} (-2 \cdot -1) \times 4 + (-2 \cdot 1 ) \times 4 + (-2 \cdot 2 ) \times 2 \\ 
			& \hspace{0.5 cm}+ (-1)^2 \times \left(  3 +  2 + 1\right) + (-1 \cdot 1) \times 4 \times 4  + (-1 \cdot 2 ) \times 4 \times 2 \\ 
			& \hspace{0.5 cm}+ (1)^2 \times \left(   3 +  2 + 1\right) + (1 \cdot 2 ) \times 4 \times 2 \\ 
			& \hspace{0.5 cm} + 2 \cdot 2 \\ \\
			&= -4 + -8 + - 26 + 22 +4\\
			&= -12
		\end{split}
		\label{eq:simple_sym_eec}
	\end{equation} 
	Clearly a symmetric distribution alone does not garuntee a full cancellation. 
	For a full proof of this for a gaussian distribution see appendix \ref{ch:symm_sample}.
\chapter{The Power of the ENC: $\alpha_s$ at the few GeV scale}
\label{ch:alphas}
	As had been shown recently at CMS \cite{CMS_alphaS}, taking the ratio of the two and (projective) three point energy correlator results in a distribution that scales directly with $\alpha_s(Q^2) R_L $ where $Q^2$ is the scale of the process under consideration. 
	To make this calculation in the measurement presented in this thesis, we need to fix the hard scale relevant to the event. 
	Here it is instructive to consider how we can adapt the methods utilized for a jet based energy correlator measurment to those for an event based model. 
	In the CMS analysis is \cite{CMS_alphaS}, they use a definition of the perturbrative scale and hard scale (where $x_L \equiv R_L$ in this thesis) as
	\begin{equation}
		\begin{split}
			PS &= \frac{p^{jet}_{T}R}{2} \\
			Q^2 &=\left(a p_{T}^{jet} x_L \right)^2 
		\end{split}
	\end{equation}
	
\chapter{Event Classification} 
\label{ch:qgdiscr}
	The more standard method of measuring $\alpha_S$ requires that one be able to accurately determine the relative quark and gluon jet populations in the event samples \cite{LeBlanc2023}. 
	While the discussion in chapter \ref{ch:alphas} allow for us to circumvent this issue in the immediate measurement of the strong force coulpling, it is still a useful quantity to measure for further studies on jet substructure measurements.
	So while it is inapproriate to tag individual jets or events as a "quark" or "gluon" type event through the Energy Correlator, given that any individual jet may have fluctuations that overlap into 
\chapter{Comparison is the Theft of Joy: What does the LHC say, and how about STAR?}
\chaptermark{LHC and STAR Results}
\chapter{Entanglement and other Lofty Goals}
\label{ch:too_much}
	In this thesis, I have presented a limited scope measurement of a whole event energy correlator in p+p event which still empowers us to use it to measure $\alpha_S$ and to estimate our gluon and quark jet populations. 

\part{Wrapping it all up}
\chapter{Observable prospects for p+p in run 24, AuAu with run 24 and 25 and the EIC}
\chaptermark{Prospects for EEC at RHIC and EIC}
\label{ch:furture_prospects}
\chapter{Remaining Questions}
\chapter{Implementation and application for the remaining sPHENIX data}
\chaptermark{Future Work}
\singlespacing
\part{Odds and Ends-Matter}
\printbibliography
%\bibliographystyle{apalike}
%\bibliography{thesis}
\begin{appendices}
	\include{Apendicies}
\end{appendices}
\end{document}
